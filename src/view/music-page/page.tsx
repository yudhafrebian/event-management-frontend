import * as React from 'react';

interface IMusicViewProps {
}

const MusicView: React.FunctionComponent<IMusicViewProps> = (props) => {
  return (
    <div>
        
    </div>
  )
};

export default MusicView;
