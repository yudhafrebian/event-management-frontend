import OrganizerView from '@/view/organizer-page/page';
import * as React from 'react';

interface IOrganizerPageProps {
}

const OrganizerPage: React.FunctionComponent<IOrganizerPageProps> = (props) => {
  return (
    <OrganizerView />
  )
};

export default OrganizerPage;
